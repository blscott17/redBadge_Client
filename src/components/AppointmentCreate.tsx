import React, { Component } from 'react';
import './style.css';
import APIURL from '../helpers/environment';

type AppointmentCreateProps = {
  
}
 
type AppointmentCreateState = {
  
}
 
 export default class AppointmentCreate extends React.Component<AppointmentCreateProps, AppointmentCreateState> {
  constructor(props: AppointmentCreateProps) {
    super(props);
    this.state = {   };
  }
  render() { 
    return ( 
      <div></div>
     );
  }
}
 
 